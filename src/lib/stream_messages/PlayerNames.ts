export interface PlayerNames {}

export function parsePlayerNamesStreamMessage(data: string): PlayerNames {
    // Not sure what the values of the message mean,
    // so just pass an empty object for now.
    return {};
}
